# instagram-osint-tool
This command-line tool extracts key profile data from Instagram for OSINT purposes. It allows you to retrieve basic profile information such as username, bio, followers count, and profile picture. The tool also saves the session for future logins and exports the extracted data to a JSON file for easy analysis.
